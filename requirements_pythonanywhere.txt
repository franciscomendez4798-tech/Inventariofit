# ══════════════════════════════════════════════════════════════════════════════
# requirements_pythonanywhere.txt
# Dependencias compatibles con PythonAnywhere Free (Python 3.10)
#
# PythonAnywhere Free usa Python 3.10 — localmente usamos 3.14.
# Este archivo fija versiones que funcionan en Python 3.10.
# ══════════════════════════════════════════════════════════════════════════════

# ── Framework Web ─────────────────────────────────────────────────────────────
Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-Migrate==4.0.7
Flask-WTF>=1.2.0
Flask-Limiter>=3.5.0
Werkzeug==3.0.3
python-dotenv==1.0.1

# ── WSGI / Servidor ───────────────────────────────────────────────────────────
# PythonAnywhere usa su propio servidor (no necesita gunicorn)
# pero lo incluimos por si se usa el script entrypoint.sh
gunicorn>=20.1.0
setuptools>=65.0

# ── Base de datos ─────────────────────────────────────────────────────────────
# SQLite incluido en Python (sin dependencia extra)
# MySQL (si usas la BD gratuita de PythonAnywhere):
# mysql-connector-python>=8.0.0
# PyMySQL>=1.1.0

# ── Generación de documentos ──────────────────────────────────────────────────
openpyxl>=3.1.2
reportlab>=4.0.0
Pillow>=10.0.0
python-docx>=1.1.0
docxtpl>=0.16.7
pypdf>=4.0.0
pdfplumber>=0.10.0
requests>=2.31.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-flask>=1.3.0
